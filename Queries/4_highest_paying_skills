/*
Aim : Identifying the top skills based on compensation
- Find the average sallary asociated with each skill for Data Analyst positions
*/

SELECT  skills,
        ROUND(AVG(salary_year_avg),0) AS avg_salary
FROM job_postings_fact
INNER JOIN skills_job_dim USING(job_id)
INNER JOIN skills_dim USING(skill_id)
WHERE job_title_short = 'Data Analyst' 
        AND salary_year_avg IS NOT NULL
        AND job_work_from_home = True
GROUP BY skills
ORDER BY avg_salary DESC
LIMIT 25

/*
Here is a breakdown of the results for high paying skills 
AI and Machine Learning tools (TensorFlow, PyTorch, Keras, Hugging Face) appear among the highest-paying skills, showing that analysts with ML capabilities tend to earn more.

Data engineering and infrastructure technologies (Kafka, Airflow, Cassandra, Terraform) are highly valued, indicating that companies pay more for analysts who can handle large-scale data systems and pipelines.

Specialized or niche technologies (Golang, Solidity, Scala) often have higher salaries because fewer professionals have these skills, increasing their market value.
*/